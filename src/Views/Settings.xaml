<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="IPAbuyer.Views.Settings"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:IPAbuyer"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <ScrollViewer>
            <StackPanel Margin="100,20,100,20">
                <Grid VerticalAlignment="Top">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Margin="0" FontSize="40" FontWeight="Bold">
                        设置
                    </TextBlock>
                    <Button Grid.Column="1" Margin="0,0,0,0" VerticalAlignment="Top" HorizontalAlignment="Right" MinWidth="120" FontSize="14" Click="BackToMainpage">
                        返回首页
                    </Button>
                </Grid>

                <TextBlock Margin="20,20,0,0" FontSize="20">
                    软件相关
                </TextBlock>
                <Grid Margin="40,20,40,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="5*"/>
                        <ColumnDefinition Width="3*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock VerticalAlignment="Center" Grid.Column="0" FontSize="16" TextWrapping="Wrap">
                        点击跳转按钮以了解本软件开发者的更多信息
                    </TextBlock>
                    <Button HorizontalAlignment="Right" Grid.Column="1" MinWidth="120" FontSize="14" Click="GithubButton">
                        访问官方网站
                    </Button>
                </Grid>
                <Grid Margin="40,20,40,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="5*"/>
                        <ColumnDefinition Width="3*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock VerticalAlignment="Center" Grid.Column="0" FontSize="16" TextWrapping="Wrap">
                        本地数据库保存了已购买的App列表，以便展示App是否已购买
                    </TextBlock>
                    <Button Foreground="White" Background="Red" HorizontalAlignment="Right" Grid.Column="1" MinWidth="120" FontSize="14" Click="DeleteDataBase">
                        清除本地数据库
                    </Button>
                </Grid>
                <TextBlock Margin="20,20,0,0" FontSize="20">
                    常见问题
                </TextBlock>
                <ItemsControl x:Name="FaqItemsControl" Margin="0" ItemsSource="{x:Bind FaqItems, Mode=OneWay}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Margin="40,20,40,0">
                                <TextBlock FontSize="16" TextWrapping="Wrap" Text="{Binding Question}"/>
                                <TextBlock Margin="20,6,0,0" FontSize="16" FontWeight="Light" TextWrapping="Wrap" Text="{Binding Answer}"/>
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                <TextBlock x:Name="FaqStatusText" Margin="40,20,40,0" FontSize="16" FontWeight="Light" Foreground="Red" TextWrapping="Wrap" Visibility="Collapsed"/>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
