<?xml version="1.0" encoding="UTF-8"?>
<Page x:Class="IPAbuyer.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:IPAbuyer"
    xmlns:models="using:IPAbuyer.Models"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Page.Content>
        <Grid Margin="50,50,50,20">
            <!-- 搜索区 -->
            <Grid HorizontalAlignment="Center"
                VerticalAlignment="Top">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="3*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <TextBox Grid.Column="0"
                    x:Name="AppNameBox"
                    Width="300"
                    Height="20"
                    PlaceholderText="请输入App名称"
                    IsSpellCheckEnabled="False"
                    KeyUp="Search_KeyUp"
                    TextChanged="AppNameBox_TextChanged" />
                <Button Grid.Column="1"
                    x:Name="SearchButton"
                    Click="SearchButton_Click"
                    Margin="16,0,0,0"
                    x:Uid="/MainPage/Search_Button" />
                <TextBlock Grid.Column="2"
                    x:Name="CountryCodeShowTextBlock"
                    VerticalAlignment="Center"
                    Style="{ThemeResource CaptionTextBlockStyle}"
                    Margin="16,0,0,0"
                    Text="国家/地区代码：cn" />
            </Grid>

            <!-- 操作按钮区 -->
            <Grid HorizontalAlignment="Left"
                VerticalAlignment="Top"
                Margin="0,60,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0"
                    Margin="0,0,0,0"
                    x:Name="BatchPurchaseButton"
                    Click="BatchPurchaseButton_Click"
                    x:Uid="/MainPage/Purchase_Button" />
                <!-- <Button Grid.Column="1"
                    Margin="10,0,0,0"
                    x:Name="BatchDownloadButton"
                    Click="BatchDownloadButton_Click"
                    x:Uid="/MainPage/Download_Button" /> -->
                <ComboBox Grid.Column="1"
                    Margin="10,0,0,0"
                    x:Name="ScreeningComboBox"
                    SelectedIndex="0"
                    SelectionChanged="ScreeningComboBox_SelectionChanged">
                    <ComboBoxItem x:Name="All"
                        Tag="All">筛选</ComboBoxItem>
                    <ComboBoxItem x:Name="OnlyNotPurchased"
                        Tag="OnlyNotPurchased">仅未购买</ComboBoxItem>
                    <ComboBoxItem x:Name="OnlyPurchased"
                        Tag="OnlyPurchased">仅已购买</ComboBoxItem>
                    <ComboBoxItem x:Name="OnlyHad"
                        Tag="OnlyHad">仅已拥有</ComboBoxItem>
                </ComboBox>
            </Grid>

            <!-- 表格区 -->
            <Border CornerRadius="2"
                Background="#80F0F8FF"
                BorderBrush="White"
                BorderThickness="1"
                Margin="0,110,0,20">
                <ListView x:Name="ResultList"
                    Margin="0,10,0,10"
                    SelectionMode="Multiple">
                    <ListView.Header>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="2*" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="2*" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="App名称"
                                Grid.Column="0"
                                HorizontalAlignment="Center">
                            </TextBlock>
                            <TextBlock Text="AppID"
                                Grid.Column="1"
                                HorizontalAlignment="Center">
                            </TextBlock>
                            <TextBlock Text="包名"
                                Grid.Column="2"
                                HorizontalAlignment="Center">
                            </TextBlock>
                            <TextBlock Text="版本"
                                Grid.Column="3"
                                HorizontalAlignment="Center">
                            </TextBlock>
                            <TextBlock Text="价格"
                                Grid.Column="4"
                                HorizontalAlignment="Center">
                            </TextBlock>
                            <TextBlock Text="状态"
                                Grid.Column="5"
                                HorizontalAlignment="Center">
                            </TextBlock>
                        </Grid>
                    </ListView.Header>
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="models:AppResult">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="2*" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="2*" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0"
                                    Text="{x:Bind name}"
                                    TextTrimming="CharacterEllipsis"
                                    ToolTipService.ToolTip="{x:Bind name}"
                                    HorizontalAlignment="Left">
                                </TextBlock>
                                <TextBlock Grid.Column="1"
                                    Text="{x:Bind id}"
                                    HorizontalAlignment="Center">
                                </TextBlock>
                                <TextBlock Grid.Column="2"
                                    Text="{x:Bind bundleID}"
                                    TextTrimming="CharacterEllipsis"
                                    ToolTipService.ToolTip="{x:Bind bundleID}"
                                    HorizontalAlignment="Center">
                                </TextBlock>
                                <TextBlock Grid.Column="3"
                                    Text="{x:Bind version}"
                                    HorizontalAlignment="Center">
                                </TextBlock>
                                <TextBlock Grid.Column="4"
                                    Text="{x:Bind price}"
                                    HorizontalAlignment="Center">
                                </TextBlock>
                                <TextBlock Grid.Column="5"
                                    Text="{x:Bind purchased}"
                                    HorizontalAlignment="Center">
                                </TextBlock>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Border>

        </Grid>
    </Page.Content>
</Page>