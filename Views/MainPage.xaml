<?xml version="1.0" encoding="utf-8"?>
<Page x:Class="IPAbuyer.Views.MainPage" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:models="using:IPAbuyer.Models" xmlns:local="using:IPAbuyer.Views" xmlns:tv="using:WinUI.TableView" mc:Ignorable="d">
    <Page.Resources>
        <CornerRadius x:Key="MainUnifiedCornerRadius">8</CornerRadius>
        <local:MainPagePriceForegroundConverter x:Key="MainPagePriceForegroundConverter" />
        <local:MainPagePurchasedForegroundConverter x:Key="MainPagePurchasedForegroundConverter" />
    </Page.Resources>
    <Page.Background>Transparent</Page.Background>
    <Page.Content>
        <Grid Padding="{StaticResource AppPagePadding}">
            <Grid RowSpacing="12">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid Grid.Row="0" ColumnSpacing="10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="10">
                        <Button x:Uid="MainPage/Action/BatchPurchaseButton" x:Name="BatchPurchaseButton" Click="BatchPurchaseButton_Click" />
                        <Button x:Uid="MainPage/Action/AddToQueueButton" x:Name="AddToDownloadQueueButton" Click="AddToDownloadQueueButton_Click" />
                        <Button x:Uid="MainPage/Action/OpenLogButton" x:Name="OpenLogButton" Click="ShowHomeLogDialog_Click" />
                        <ComboBox x:Name="ScreeningComboBox" MinHeight="36" MinWidth="132" SelectedIndex="0" SelectionChanged="ScreeningComboBox_SelectionChanged">
                            <ComboBoxItem x:Uid="MainPage/Filter/AllItem" x:Name="All" Tag="All"></ComboBoxItem>
                            <ComboBoxItem x:Uid="MainPage/Filter/OnlyNotPurchasedItem" x:Name="OnlyNotPurchased" Tag="OnlyNotPurchased"></ComboBoxItem>
                            <ComboBoxItem x:Uid="MainPage/Filter/OnlyPurchasedItem" x:Name="OnlyPurchased" Tag="OnlyPurchased"></ComboBoxItem>
                            <ComboBoxItem x:Uid="MainPage/Filter/OnlyOwnedItem" x:Name="OnlyHad" Tag="OnlyHad"></ComboBoxItem>
                        </ComboBox>
                    </StackPanel>
                    <Button Grid.Column="1" x:Uid="MainPage/Action/CancelAllDownloadsButton" x:Name="CancelAllDownloadsButton" Click="CancelAllDownloadsButton_Click" Visibility="Collapsed" />
                </Grid>
                <Border Grid.Row="1" Style="{StaticResource AppSurfaceBorderStyle}">
                    <Grid>
                        <tv:TableView
                            x:Name="ResultList"
                            AutoGenerateColumns="False"
                            Background="Transparent"
                            CanFilterColumns="False"
                            CanReorderColumns="False"
                            CanResizeColumns="True"
                            CanSortColumns="True"
                            CornerButtonMode="None"
                            GridLinesVisibility="None"
                            HeaderGridLinesVisibility="None"
                            HeaderRowHeight="48"
                            IsMultiSelectCheckBoxEnabled="True"
                            IsReadOnly="True"
                            RowHeight="60"
                            RowMinHeight="52"
                            SelectionMode="Multiple"
                            SelectionUnit="Row">
                            <tv:TableView.RowContextFlyout>
                                <MenuFlyout>
                                    <MenuFlyoutItem x:Uid="MainPage/Context/PurchaseItem" Click="ContextMenuPurchase_Click" />
                                    <MenuFlyoutItem x:Uid="MainPage/Context/AddToQueueItem" Click="ContextMenuAddToQueue_Click" />
                                    <MenuFlyoutSeparator />
                                    <MenuFlyoutItem x:Uid="MainPage/Context/MarkNotPurchasedItem" Click="ContextMenuMarkNotPurchased_Click" />
                                    <MenuFlyoutItem x:Uid="MainPage/Context/MarkPurchasedItem" Click="ContextMenuMarkPurchased_Click" />
                                    <MenuFlyoutItem x:Uid="MainPage/Context/MarkOwnedItem" Click="ContextMenuMarkOwned_Click" />
                                    <MenuFlyoutSeparator />
                                    <MenuFlyoutItem x:Uid="MainPage/Context/CopyNameItem" Click="ContextMenuCopyName_Click" />
                                    <MenuFlyoutItem x:Uid="MainPage/Context/CopyIdItem" Click="ContextMenuCopyId_Click" />
                                    <MenuFlyoutItem x:Uid="MainPage/Context/CopyVersionItem" Click="ContextMenuCopyVersion_Click" />
                                </MenuFlyout>
                            </tv:TableView.RowContextFlyout>
                            <tv:TableView.Columns>
                                <tv:TableViewTemplateColumn x:Name="NameColumn" Width="3*" MinWidth="240" SortMemberPath="name" CanFilter="False">
                                    <tv:TableViewTemplateColumn.CellTemplate>
                                        <DataTemplate x:DataType="models:SearchResult">
                                            <StackPanel Orientation="Horizontal" Spacing="8">
                                                <Border Width="24" Height="24" CornerRadius="{StaticResource MainUnifiedCornerRadius}" Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}" BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}" BorderThickness="0.6">
                                                    <Image Width="24" Height="24" Source="{x:Bind artworkUrl}" Stretch="UniformToFill" />
                                                </Border>
                                                <TextBlock VerticalAlignment="Center" Text="{x:Bind name}" TextTrimming="CharacterEllipsis" ToolTipService.ToolTip="{x:Bind name}" />
                                            </StackPanel>
                                        </DataTemplate>
                                    </tv:TableViewTemplateColumn.CellTemplate>
                                </tv:TableViewTemplateColumn>
                                <tv:TableViewTextColumn x:Name="IdColumn" Width="*" MinWidth="120" Binding="{Binding id}" SortMemberPath="id" CanFilter="False" />
                                <tv:TableViewTextColumn x:Name="DeveloperColumn" Width="2*" MinWidth="180" Binding="{Binding developer}" SortMemberPath="developer" CanFilter="False" />
                                <tv:TableViewTextColumn x:Name="VersionColumn" Width="*" MinWidth="120" Binding="{Binding version}" SortMemberPath="version" CanFilter="False" />
                                <tv:TableViewTemplateColumn x:Name="PriceColumn" Width="*" MinWidth="100" SortMemberPath="price" CanFilter="False">
                                    <tv:TableViewTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock HorizontalAlignment="Center" Text="{Binding price}" Foreground="{Binding price, Converter={StaticResource MainPagePriceForegroundConverter}}" />
                                        </DataTemplate>
                                    </tv:TableViewTemplateColumn.CellTemplate>
                                </tv:TableViewTemplateColumn>
                                <tv:TableViewTemplateColumn x:Name="PurchasedColumn" Width="*" MinWidth="120" SortMemberPath="purchased" CanFilter="False">
                                    <tv:TableViewTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock HorizontalAlignment="Center" Text="{Binding purchased}" Foreground="{Binding purchased, Converter={StaticResource MainPagePurchasedForegroundConverter}}" />
                                        </DataTemplate>
                                    </tv:TableViewTemplateColumn.CellTemplate>
                                </tv:TableViewTemplateColumn>
                            </tv:TableView.Columns>
                        </tv:TableView>
                        <ProgressRing
                            x:Name="TableLoadingRing"
                            Width="44"
                            Height="44"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            IsActive="False"
                            IsHitTestVisible="False"
                            Visibility="Collapsed"
                            Canvas.ZIndex="10" />
                    </Grid>
                </Border>
                <InfoBar x:Name="HomeStatusInfoBar" Grid.Row="2" IsOpen="False" IsClosable="True" Severity="Informational" />
            </Grid>
        </Grid>
    </Page.Content>
</Page>
