<?xml version="1.0" encoding="utf-8"?>
<Page x:Class="IPAbuyer.Views.MainPage" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:models="using:IPAbuyer.Models" xmlns:controls="using:CommunityToolkit.WinUI.Controls" mc:Ignorable="d">
    <Page.Resources>
        <CornerRadius x:Key="MainUnifiedCornerRadius">8</CornerRadius>
    </Page.Resources>
    <Page.Content>
        <Grid Margin="16" RowSpacing="12">
            <Grid RowSpacing="12">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <controls:SettingsCard Grid.Row="0">
                    <controls:SettingsCard.Header>
                        <StackPanel Orientation="Horizontal" Spacing="10">
                            <Button x:Uid="MainPage/Action/BatchPurchaseButton" x:Name="BatchPurchaseButton" Click="BatchPurchaseButton_Click" />
                            <Button x:Uid="MainPage/Action/AddToQueueButton" x:Name="AddToDownloadQueueButton" Click="AddToDownloadQueueButton_Click" />
                            <ComboBox x:Name="ScreeningComboBox" MinHeight="36" MinWidth="132" SelectedIndex="0" SelectionChanged="ScreeningComboBox_SelectionChanged">
                                <ComboBoxItem x:Uid="MainPage/Filter/AllItem" x:Name="All" Tag="All"></ComboBoxItem>
                                <ComboBoxItem x:Uid="MainPage/Filter/OnlyNotPurchasedItem" x:Name="OnlyNotPurchased" Tag="OnlyNotPurchased"></ComboBoxItem>
                                <ComboBoxItem x:Uid="MainPage/Filter/OnlyPurchasedItem" x:Name="OnlyPurchased" Tag="OnlyPurchased"></ComboBoxItem>
                                <ComboBoxItem x:Uid="MainPage/Filter/OnlyOwnedItem" x:Name="OnlyHad" Tag="OnlyHad"></ComboBoxItem>
                            </ComboBox>
                            <Button x:Uid="MainPage/Action/OpenLogButton" x:Name="OpenLogButton" Click="ShowHomeLogDialog_Click" />
                        </StackPanel>
                    </controls:SettingsCard.Header>
                </controls:SettingsCard>
                <Border Grid.Row="1" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="{StaticResource MainUnifiedCornerRadius}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <ProgressBar x:Name="PurchaseLoadingBar" Grid.Row="0" Height="3" IsIndeterminate="True" IsHitTestVisible="False" Visibility="Collapsed" />
                        <Grid Grid.Row="1" Padding="14,8,14,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="32" />
                                <ColumnDefinition Width="3*" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="2*" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Button x:Uid="MainPage/Header/NameButton" x:Name="NameHeaderText" Grid.Column="1" Background="Transparent" BorderThickness="0" MinHeight="24" Padding="0" HorizontalAlignment="Center" Click="SortHeader_Click" Tag="name" />
                            <Button x:Uid="MainPage/Header/IdButton" x:Name="IdHeaderText" Grid.Column="2" Background="Transparent" BorderThickness="0" MinHeight="24" Padding="0" HorizontalAlignment="Center" Click="SortHeader_Click" Tag="id" />
                            <Button x:Uid="MainPage/Header/DeveloperButton" x:Name="DeveloperHeaderText" Grid.Column="3" Background="Transparent" BorderThickness="0" MinHeight="24" Padding="0" HorizontalAlignment="Center" Click="SortHeader_Click" Tag="developer" />
                            <Button x:Uid="MainPage/Header/VersionButton" x:Name="VersionHeaderText" Grid.Column="4" Background="Transparent" BorderThickness="0" MinHeight="24" Padding="0" HorizontalAlignment="Center" Click="SortHeader_Click" Tag="version" />
                            <Button x:Uid="MainPage/Header/PriceButton" x:Name="PriceHeaderText" Grid.Column="5" Background="Transparent" BorderThickness="0" MinHeight="24" Padding="0" HorizontalAlignment="Center" Click="SortHeader_Click" Tag="price" />
                            <Button x:Uid="MainPage/Header/PurchasedButton" x:Name="PurchasedHeaderText" Grid.Column="6" Background="Transparent" BorderThickness="0" MinHeight="24" Padding="0" HorizontalAlignment="Center" Click="SortHeader_Click" Tag="purchased" />
                        </Grid>
                        <ListView x:Name="ResultList" Grid.Row="2" Margin="0,0,0,8" ContainerContentChanging="ResultList_ContainerContentChanging" IsMultiSelectCheckBoxEnabled="True" SelectionMode="Multiple">
                            <ListView.ItemTemplate>
                                <DataTemplate x:DataType="models:SearchResult">
                                    <Grid Margin="-40,0,0,0" Padding="52,12,14,12">
                                        <Grid.ContextFlyout>
                                            <MenuFlyout>
                                                <MenuFlyoutItem x:Uid="MainPage/Context/PurchaseItem" Click="ContextMenuPurchase_Click" />
                                                <MenuFlyoutItem x:Uid="MainPage/Context/AddToQueueItem" Click="ContextMenuAddToQueue_Click" />
                                                <MenuFlyoutSeparator />
                                                <MenuFlyoutItem x:Uid="MainPage/Context/MarkNotPurchasedItem" Click="ContextMenuMarkNotPurchased_Click" />
                                                <MenuFlyoutItem x:Uid="MainPage/Context/MarkPurchasedItem" Click="ContextMenuMarkPurchased_Click" />
                                                <MenuFlyoutItem x:Uid="MainPage/Context/MarkOwnedItem" Click="ContextMenuMarkOwned_Click" />
                                                <MenuFlyoutSeparator />
                                                <MenuFlyoutItem x:Uid="MainPage/Context/CopyNameItem" Click="ContextMenuCopyName_Click" />
                                                <MenuFlyoutItem x:Uid="MainPage/Context/CopyIdItem" Click="ContextMenuCopyId_Click" />
                                                <MenuFlyoutItem x:Uid="MainPage/Context/CopyVersionItem" Click="ContextMenuCopyVersion_Click" />
                                            </MenuFlyout>
                                        </Grid.ContextFlyout>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="32" />
                                            <ColumnDefinition Width="3*" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="2*" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8">
                                            <Border Width="24" Height="24" CornerRadius="{StaticResource MainUnifiedCornerRadius}" Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}" BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}" BorderThickness="0.6">
                                                <Image Width="24" Height="24" Source="{x:Bind artworkUrl}" Stretch="UniformToFill" />
                                            </Border>
                                            <TextBlock VerticalAlignment="Center" Text="{x:Bind name}" TextTrimming="CharacterEllipsis" ToolTipService.ToolTip="{x:Bind name}" />
                                        </StackPanel>
                                        <TextBlock Grid.Column="2" HorizontalAlignment="Center" Text="{x:Bind id}" />
                                        <TextBlock Grid.Column="3" HorizontalAlignment="Center" Text="{x:Bind developer}" TextTrimming="CharacterEllipsis" ToolTipService.ToolTip="{x:Bind developer}" />
                                        <TextBlock Grid.Column="4" HorizontalAlignment="Center" Text="{x:Bind version}" />
                                        <TextBlock x:Name="PriceTextBlock" Grid.Column="5" HorizontalAlignment="Center" Text="{x:Bind price}" />
                                        <TextBlock x:Name="PurchasedTextBlock" Grid.Column="6" HorizontalAlignment="Center" Text="{x:Bind purchased}" />
                                    </Grid>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </Page.Content>
</Page>
